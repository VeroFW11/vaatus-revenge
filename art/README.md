# Art source files

- `characters/` — Blender character files
- `environments/` — Blender props, buildings, terrain pieces
- `concept/` — concept art and mood boards
- `reference/` — reference images

Lock `.blend` and `.psd` files before editing (`git lfs lock <file>`). Export FBX into `game/Assets/` for Unity.
